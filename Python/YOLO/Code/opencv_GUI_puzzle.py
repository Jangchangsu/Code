import cv2 as cv
import os
import time
from elice_utils import E
directory = "C:\\Users\\jangh\\Desktop\\Code\\Python\\YOLO"
Image_directory = os.path.join(directory,"Image")
file0 = os.path.join(Image_directory,"Lenna.png")

def solve_puzzle(img, piece_order):
    
    return img

piece_order = list(map(int, list(sys.argv[1].split(','))))

img = cv2.imread("")